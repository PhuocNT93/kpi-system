# Mandatory Agent Entry Point

Before responding to, planning, investigating, editing, testing, reviewing, or running commands for any task, read and follow `usage.md`.

`usage.md` is mandatory. It requires the workflow in `docs/AI_AGENT_WORKFLOW.md`, beginning at Step 0 and stopping for explicit user approval after every step.

Do not continue beyond the current workflow step until the user explicitly approves it.